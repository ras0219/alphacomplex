Alpha Complex
=============

### Compilation

Compile in a separate directory.

    $ mkdir build
    $ cd build
    $ cmake ..
    $ make

### Execution

Currently, cities are read from stdin.

    $ ./alphacomplex < ../cities/city02.txt
